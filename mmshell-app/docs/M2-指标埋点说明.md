# M2 指标埋点说明（Day057）

## 运行时指标

- `totalChecks`：连接测试总次数
- `successChecks`：连接成功次数
- `failedChecks`：连接失败次数
- `totalRetries`：累计重试次数
- `totalLatencyMs`：累计连接耗时
- `lastError`：最近错误信息

## 数据来源

- Rust 后端 `RuntimeMetricsStore`
- 接口：`get_runtime_metrics`

## 前端展示

- 终端面板底部显示汇总指标与平均延迟
- 每次轮询会同步最新指标

